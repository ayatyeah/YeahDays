# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright / test artifacts
/.playwright-mcp
/shot-*.png

# local MCP config — содержит секреты (напр. FIGMA_API_KEY)
/.mcp.json

# журнал импорта из LMS — какие UID уже залиты (scripts/lms-import.mjs).
# Локальное состояние конкретной машины, а не код: в репозитории он привёл
# бы к тому, что чужой прогон считается своим и дедлайны не импортируются.
/.lms-import-state.json

# выгрузки календаря Moodle — расписание конкретного человека
/*.ics
