import { handlers } from "@/auth";

/**
 * Маршрут Auth.js. Здесь какое-то время жила диагностика входа через
 * Google (логировала имена кук в колбэке) — убрана: на каждый запрос она
 * писала в лог Railway, а причину так и не показала.
 */
export const { GET, POST } = handlers;
